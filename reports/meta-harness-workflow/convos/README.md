# Curated Conversations

Sanitized prompt/response transcripts from the final two comparison runs used in [`../report.md`](../report.md).

These are not raw terminal logs. Local filesystem paths were replaced with placeholders:

- `<baseline-repo>`: local checkout used for the baseline run
- `<skill-guided-repo>`: local checkout used for the skill-guided run

Files:

- [`final-baseline.md`](final-baseline.md)
- [`final-skill-guided.md`](final-skill-guided.md)

Earlier attempts are summarized in [`../report.md`](../report.md). They are not included as curated conversations because the final report uses them only as design-failure notes.
